---
description: Keep UI presentational; data and side effects in repos / screens
globs: src/components/**/*.tsx,src/features/**/*.tsx,App.tsx
alwaysApply: false
---

# UI and data separation

Follow `doc/ui-data-separation.md`.

- `src/components/ui/*` is presentational only (props + theme + local UI state).
- Screens/containers own repository calls, notifications, and native bridges.
- Do not import `src/db` from UI kit components.
