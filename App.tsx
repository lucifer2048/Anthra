/**
 * Legacy entry — Anthra now boots through Expo Router (`expo-router/entry` → `app/_layout.tsx`).
 * Keep this file only as a reference during migration; it is not loaded at runtime.
 */
export { AppShellProvider, AppShellChrome, INITIAL_STATS, resolveModuleTheme } from "./src/app-shell/AppShellProvider";
